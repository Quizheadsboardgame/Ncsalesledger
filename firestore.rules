rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Pokémon stall sales ledger - shared across owner's devices
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
